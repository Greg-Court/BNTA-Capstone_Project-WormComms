import { getAllMessages } from "../api"

const Messages = ({stompClient}) => {

    // const conversationMessages = getAllMessages()

    // const messages = conversationMessages.map((message, index) => {
    //     return (
    //         <Message 
    //         message={message} 
    //         key={index}></Message>
    //     )
    // })

return (
    <ul>

    </ul>
)
}

export default Messages